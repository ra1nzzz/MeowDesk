# MeowDesk 依赖
# Python 3.12+

Pillow>=10.0.0
send2trash>=1.8.0
pystray>=0.19.0

# Windows 平台（内置）
# windnd - 拖拽支持，需单独安装
# pip install windnd

# macOS 平台（可选）
# pyobjc-framework-Cocoa>=10.0
# rumps>=0.4.0
